spring:
  application:
    name: account-ledger-service
  datasource:
    url: jdbc:mysql://localhost:3306/ledger
    username: ledger
    password: ledger
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

logging:
  level:
    com.example.exchange.accountledger: INFO
