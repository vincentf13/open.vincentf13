# =========================================================
# Maven 全域高效構建配置 (maven.config)
# Maven 會在每次執行 mvn/mvnw 時自動載入這些參數
# =========================================================

# 使用 CPU 核心數，提升多模組併行效率
-T 1C

# 關閉傳輸進度條，減少 I/O 與日誌干擾
-ntp

# 採用共享 Build Cache，減少重複編譯
-Dmaven.build.cache.enabled=true
-Dmaven.build.cache.directory=${user.home}/.m2/maven-build-cache

# 提高 Artifact 下載併行度與 HTTP 連線池
-Dmaven.artifact.threads=32
-Dmaven.wagon.httpconnectionManager.maxTotal=128
-Dmaven.wagon.httpconnectionManager.maxPerRoute=32

# 啟用增量編譯，提高重複構建速度
-Dmaven.compiler.useIncrementalCompilation=true

# 簡化傳輸日誌輸出，避免阻塞
-Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=warn

# =========================================================
# 構建優化：跳過非必要任務（若需要再取消註解）
# =========================================================

# 完全跳過測試編譯與執行，構建時間最短
# -Dmaven.test.skip=true

# 跳過 Javadoc 文件生成
-Dmaven.javadoc.skip=true

# 跳過程式碼風格檢查 (Checkstyle)
-Dcheckstyle.skip=true

# 跳過構建規則檢查 (Enforcer Plugin)
-Denforcer.skip=true
