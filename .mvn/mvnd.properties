# =========================================================
# Maven Daemon (mvnd) 全域高效構建配置
# =========================================================

# 使用 CPU 核心數自動分配執行緒（等價於 -T 1C）
mvnd.threads=1C

# 啟用 Maven Build Cache（等價於 -Dmaven.build.cache.enabled=true）
mvnd.build.cache.enabled=true

# 並行下載 Artifact 數量（等價於 -Dmaven.artifact.threads=20）
mvnd.artifact.threads=20

# 啟用增量編譯（等價於 -Dmaven.compiler.useIncrementalCompilation=true）
mvnd.compiler.useIncrementalCompilation=true

# =========================================================
# 构建優化：跳過非必要步驟
# =========================================================

# 完全跳過測試編譯與執行
maven.test.skip=true

# 跳過 Javadoc 文件生成
maven.javadoc.skip=true

# 跳過程式碼風格檢查 (Checkstyle)
checkstyle.skip=true

# 跳過構建規則檢查 (Enforcer Plugin)
enforcer.skip=true

# =========================================================
# JVM 參數（可依實際記憶體調整）
# =========================================================
mvnd.jvmArgs=-Xmx2G -XX:+UseG1GC

# Daemon 快取存放路徑
mvnd.daemonStorage=~/.m2/registry
