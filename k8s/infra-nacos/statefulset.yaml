apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: infra-nacos
  labels:
    app: infra-nacos
spec:
  serviceName: infra-nacos-headless
  replicas: 1
  selector:
    matchLabels:
      app: infra-nacos
  template:
    metadata:
      labels:
        app: infra-nacos
    spec:
      containers:
        - name: nacos
          image: nacos/nacos-server:v2.3.2
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8848
            - name: grpc
              containerPort: 9848
            - name: grpc-client
              containerPort: 9849
          env:
            - name: MODE
              value: standalone
            - name: STORAGE_MODE
              value: embedded  # 如果你有跑 infra-mysql，這裡建議改成 "mysql" 並配置 DB 連線，這樣資料才不會因為 Pod 重建而消失
            - name: NACOS_AUTH_ENABLE
              value: "false"
            # --- 記憶體優化區 ---
            - name: JVM_XMS
              value: 256m
            - name: JVM_XMX
              value: 384m      # [關鍵優化] 留 128MB 給 Metaspace 和 Threads，避免被 K8s 殺掉
            - name: JVM_XMN
              value: 128m
            - name: JAVA_OPT_EXT
              value: "-XX:+UseSerialGC -XX:MaxDirectMemorySize=64m" # [關鍵優化] 使用 SerialGC 省 CPU/RAM，限制 Netty 直接記憶體
            # ------------------
            - name: PREFER_HOST_MODE
              value: hostname
          volumeMounts:
            - name: nacos-data
              mountPath: /home/nacos/data
              subPath: data
            - name: nacos-logs
              mountPath: /home/nacos/logs
            - name: nacos-config
              mountPath: /home/nacos/init.d/custom.properties
              subPath: custom.properties
          # --- 探針優化：開發環境可以寬容一點 ---
          readinessProbe:
            httpGet:
              path: /nacos/v1/console/health/readiness
              port: 8848
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /nacos/v1/console/health/liveness
              port: 8848
            initialDelaySeconds: 120
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            limits:
              memory: "512Mi" # 硬上限
            requests:
              memory: "256Mi" # 預留
      volumes:
        - name: nacos-config
          configMap:
            name: infra-nacos-config
            items:
              - key: custom.properties
                path: custom.properties
        - name: nacos-logs
          emptyDir: {}
  volumeClaimTemplates:
    - metadata:
        name: nacos-data
        labels:
          app: infra-nacos
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 2Gi
        storageClassName: infra-nacos-local